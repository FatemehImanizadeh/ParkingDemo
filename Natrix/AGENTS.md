# Project context

## Authorization required

Do not modify, create, delete, rename, or move any file or directory in this folder or its subfolders without the user's explicit authorization. Ask before making changes unless the user has already explicitly authorized those specific changes. This includes documentation, generated files, and changes caused by commands or tools. Reading and inspecting files does not require approval.

## Saved project knowledge

Read `PROJECT_MAP.md` for the saved project overview, annotated file tree, active versus excluded components, and workflow before project work. The user requested that this knowledge be retained for future tasks.

The user confirmed that `GHFile/ExampleFile.gh` is the main working Grasshopper definition.

Treat the map as a dated snapshot; verify relevant source code and `ParkingDemo.csproj` before changes. Updates to affected descriptions when file responsibilities change also require the user's explicit authorization.
